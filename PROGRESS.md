# Progress

프로젝트 진행 현황 로그. 커밋 직전 갱신한다 (`rules/common/git.md` 규칙).

## 2026-06-03

- **템플릿 셋업** (`/setup-from-template`): 미사용 언어 룰(python/typescript) import·섹션 제거, 상단 seed 주석 제거, `docs/template/`·`rules/languages/` 삭제. @import 12개 검증 OK.
- **Keycloak 로컬 개발 배포 설계 확정**: Docker Compose 2-서비스(keycloak `start-dev` + postgres, named volume 영속), realm 빈 상태 시작·수동 구성, secret은 `.env` 주입. spec: `docs/superpowers/specs/2026-06-03-keycloak-docker-local-dev-design.md`. WHY: 로컬에서 앱 OIDC 연동을 운영과 동일 DB로 재현 가능하게 하기 위함.
- **구현 플랜 작성**: 6-task 플랜(env.example → .env → compose → 기동·로그인 검증 → 영속 검증 → 기록). plan: `docs/superpowers/plans/2026-06-03-keycloak-docker-local-dev.md`.
- **구현 시작**: `.env.example` 추가 (이미지 핀·관리자·DB 자격 키 정의). secret 실값은 `.env`(미커밋)로 주입.
- **compose 추가**: `docker-compose.yml` — postgres(healthcheck·volume) + keycloak(start-dev, depends_on healthy, 8080/9000). `docker compose config` 통과.
- **런타임 검증 완료**: `up -d` → postgres healthy / keycloak running, `/health/ready` UP, master realm 토큰 발급(관리자 로그인) OK, realm 생성→`restart keycloak`→잔존(200) 으로 영속 검증. 테스트 realm 정리(204).
- **README 작성**: 플레이스홀더 1줄 → 전체 문서(구성표·빠른 시작·환경 변수표·명령어·헬스 체크) 로 교체.
- **SSO 동작 GUI 검증**: `demo` realm + 테스트 사용자 생성, OIDC client 2개(`site1` localhost:3000 / `site2` localhost:3001, public·standard flow) 등록. site1 로그인 → site2 자동 로그인(SSO) 및 site1 로그아웃 → site2 세션 종료(Single Logout) 확인. WHY: 단일 realm으로 다중 앱 세션 공유가 실제로 동작하는지 로컬에서 확인하기 위함.
- **커스텀 로그인 테마 추가**: compose `keycloak` 서비스에 `./themes:/opt/keycloak/themes` 볼륨 마운트(다른 서비스/설정 불변). `themes/custom/login` 테마 생성 — `parent=keycloak.v2`, `styles=css/styles.css css/custom.css`(v2 베이스 스타일 유지 위에 Apple 스타일 오버라이드), v2 `login.ftl`/`template.ftl` 사본(검증 마커 + `apple-login` body 클래스), `custom.css`는 `docs/superpowers/apple.md` 토큰(Action Blue·SF Pro·pill CTA) 구현. `demo` realm `loginTheme=custom` 설정(kcadm). 검증: site1 로그인 폼 HTML에 `apple-login` body·마커·`styles.css`→`custom.css` 순서 확인, 테마 CSS 200 서빙. WHY: 내장 jar 테마 불변 유지하며 호스트 오버라이드로 로그인 화면 브랜딩.
- **로그인 테마 세부 조정**: `AI-portal/src/app/admin/login/page.tsx`(+ tailwind.config·apple.css) 기준으로 `custom.css` 재작성 — 카드 제거(맨 surface-alt 배경·max-w 384px), 입력 pill·h44·focus 테두리만 accent, 중앙 display-md(34px) 타이틀, fade-up 스태거 진입(ease-spring), eye 토글 ghost 버튼. Chrome headless 스크린샷으로 시각 검증.
- **연결 서비스 쇼케이스 스택 추가**: 로그인 화면을 2-컬럼으로(좌 폼 불변 + 우 쇼케이스). 우측에 브라우저 창 모양 카드를 겹쳐 쌓고 `showcase.js`가 `manifest.json`을 fetch해 렌더 후 3.5s 간격 맨 위 카드를 뒤로 보내는 오토 로테이션. `gen-services.sh`가 `resources/img/services/*.png`를 스캔해 파일명→창 타이틀 매핑으로 `manifest.json` 자동 생성(PNG 추가/삭제 후 재실행만 하면 카드 증감). 목업 PNG 4종은 headless Chrome 렌더로 생성(Python 의존성 0). 검증: 리소스 200 서빙, 좌 폼+우 스택 스크린샷, 회전 동작, 4↔5 증감, ≤900px 우측 패널 숨김. WHY: 어떤 서비스들이 이 SSO로 연결되는지 로그인 시점에 시각적으로 보여주기 위함.
- **서버 배포 플로우 문서화 + CRLF 고정 + 포트 파라미터화**: `.gitattributes` 추가(`* eol=lf`, `*.sh eol=lf`, `*.png binary`) — Mac→Windows→Linux 서버 경로에서 Windows 체크아웃(autocrlf)이 셸 스크립트 셰뱅을 CRLF로 깨뜨리는 것을 working tree LF 강제로 차단. `KEYCLOAK_HTTP_PORT`(.env, 기본 8080) 도입해 compose 포트를 `${KEYCLOAK_HTTP_PORT:-8080}:8080`로 파라미터화(서버 6502). README에 배포 경로(git push→pull→`scp -r`→서버 `.env.example`로 작성→`up -d`) 추가 — `.env`는 gitignore라 scp에 자연 제외·`.git` 포함. WHY: 로컬과 동일 리포를 서버에 안전히 옮겨 포트만 바꿔 기동하기 위함.
- **README 배포 절차 보강(권한·용량 우회 + 반복 작업 명령)**: scp를 폴더 이동 후 상대경로(`scp -r .`)로 변경. SSH 서버 권한 문제로 홈(`~/keycloak_dev`) 업로드 → `sudo mv`(최초)/`sudo rsync -a --delete --exclude='.env'`(이후)로 `/data/keycloak_dev`(용량) 동기화하는 2-경로 명령 추가. 반복 업데이트 시 `.env`·DB 볼륨 보존. WHY: 서버의 쓰기 권한/디스크 제약과 반복 배포를 실제 운영 절차에 맞춰 문서화.
- **서버 운영 명령어 문서 신설**: `docs/server-commands.md` 추가 — 서버에서 복붙용 명령 모음(`"HTTPS required"` 해제 `sslRequired=NONE` kcadm, 테마 적용 `loginTheme=custom`, 상태/로그/헬스, 재기동). 코드블록별 복사 버튼 활용. 앞으로 운영 명령은 이 문서에 누적. WHY: 서버에서 kcadm/compose 명령 복사가 번거로워 단일 복붙 시트 필요.
- **로그인 디자인 세부 조정 + 디자인 규칙 문서화**: (1) 제목 `Sign in to your account`→`Login` (`messages/messages_en.properties`로 `loginAccountTitle` 오버라이드). (2) 입력칸 placeholder 추가 — `field.ftl`을 테마로 복사해 `input`/`password` 매크로에 `placeholder` 파라미터 신설. (3) 비번 보기 버튼 24px로 축소 + 보더 제거. (4) `docs/design-rules.md` 추가 — claude_design_template `.claude/design/styling-rules.md`·apple.md 기준 핵심 규칙 요약. 로컬 8080 데모 realm 스크린샷으로 검증. WHY: 로그인 화면을 참고 디자인에 맞춰 단순화하고 규칙을 명문화.
- **CLAUDE.md 감사·보강**: 프로젝트 개요 섹션 추가(무엇인지·핵심 명령: compose 기동/헬스·테마 bind-mount·realm 적용·gen-services·문서 인덱스), "검증=런타임/스크린샷"(단위 테스트 하니스 없음, testing.md 비적용·guidelines.md 수동검증 적용) 명시, 백엔드 헤딩의 템플릿 잔여 문구("아니면 이 블록 삭제") 정리 + Dockerfile/deps 규칙 해당시 적용 주석. @import 12개 실재 확인. WHY: 새 인스턴스가 CLAUDE.md만으로 프로젝트·워크플로 파악 가능하게.
- **왼쪽 로그인 박스 진입을 쇼케이스 카드와 동일한 float-up으로**: 박스 래퍼(`__main`)가 `revealIn`(scale 0.7 zoom)으로 확대되며 뜨던 것을 `appleFadeUp`(아래에서 떠오르며 페이드인, 18px)로 변경 — 카드와 같은 효과로 통일. 자식 개별 stagger(타이틀·그룹·버튼)는 제거해 박스가 하나의 단위로 부상(부모 opacity가 자식 stagger를 가리는 문제도 해소). 미사용 `appleRevealIn` 키프레임 정리. WHY: 좌측 박스와 우측 카드의 등장 효과 통일.
- **로딩 진입 애니메이션 보강(쇼케이스 카드 float-up)**: 폼 entrance(revealIn 래퍼 + staggered fadeUp: 타이틀 50/그룹 250·320/버튼 400ms)는 이미 AI-portal `animations.css`와 동일하게 구현돼 있었음. 이번엔 우측 쇼케이스가 패널 통째로 페이드되던 것을 **카드별 staggered float-up**(각 카드가 26px 아래에서 슬롯으로 떠오르며 페이드인, 200ms+슬롯당 90ms)으로 확장해 화면 전체가 컴포넌트 단위로 떠오르게 함. `fanTransform`에 세로 오프셋 파라미터 추가, `prefers-reduced-motion` 시 즉시 배치. 진입 후 정착 스크린샷으로 덱 온전 확인. WHY: AI-portal처럼 로딩 시 컴포넌트들이 순차적으로 페이드인·부상.
- **쇼케이스 이미지 톤 통일 프롬프트 문서**: `docs/showcase-image-prompt.md` 추가 — 실제 서비스 화면(원본)을 기본 목업 톤(Apple-light: 그라데이션 배경·hairline 카드·소프트 섀도·accent 단일 hue·1280×800)으로 다시 그리되 원본 정체성(브랜드·레이아웃·고유 UI) 보존하는 복사용 프롬프트 + 스타일 스펙 + 보존 체크리스트. 사용: Claude에 원본+참고 이미지+프롬프트 전달. WHY: 사용자가 실서비스 스크린샷을 통일된 느낌으로 변환해 쇼케이스에 쓰기 위함.
- **쇼케이스 이미지 프롬프트 — Claude Code 직접 실행 방식으로 전환**: `docs/showcase-image-prompt.md` 전면 재작성. 캡처→채팅(원본+참고 목업 첨부) 방식을 폐기하고, **대상 서비스의 소스 레포에서 Claude Code가 메인 HTML을 읽어** 톤 목업 HTML 4종 작성→Chrome headless 1280×800 캡처→2×2 컨택시트 제시→**사람이 1장 선택**하는 에이전트 워크플로우로 변경. 참고 목업 이미지 제거(톤 스펙은 프롬프트 텍스트로만). 결과는 **단순화·추상화된 고스트 목업**(본문은 스켈레톤 플레이스홀더로 비우고, 레이아웃 구조·로고·시그니처 형태·brand색으로만 식별 — 스크린샷처럼 보이면 실패) + Apple-light 공통 속성(1280×800·배경 그라데이션·hairline 카드·flat·단일 accent·고스트)을 얹음. 소스 레포에서 도는 점 반영해 실데이터·시크릿 금지 가드 추가. `CLAUDE.md` 문서 인덱스 설명도 "자동 생성"으로 갱신. WHY: 사람이 스크린샷을 찍어 주는 수작업을 없애고, 캡처처럼 디테일한 화면이 아니라 단순화된 골격을 Claude Code가 소스 기반으로 만들어 고르게 하기 위함.
- **기존 운영 Keycloak 테마 적용 절차 문서**: `docs/apply-theme-existing-keycloak.md` 추가 — 별도 운영 ai-portal Keycloak에 테마만 입히는 법(서브폴더 clone + `./kc-theme/themes` 마운트 → `up -d keycloak` → realm `ai-portal` `loginTheme=custom`). 데이터/`shared-net` 보존·`down -v` 금지·레포 전체 풀 금지 명시. WHY: 실서버 적용 절차 명문화.
- **쇼케이스 뒷장 재투입을 우측 슬라이드인으로**: 맨 뒤 카드가 제자리에 갑자기 생기던 것을 **오른쪽에서 꽂혀 들어오는** 슬라이드(+기존 페이드인 유지)로 변경 — `fanTransform(slot, extraX)`에 우측 오프셋 파라미터 추가, 재투입 시 `+ENTER_DX(240px)` 위치에서 백 슬롯으로 이동. 타이밍은 앞장이 사라진 뒤 **0.5초 텀**(`DROP_MS + ENTER_DELAY`) 후 시작. 1회전 정착 스크린샷으로 덱 온전 확인. WHY: 재투입 등장을 더 자연스럽게.
- **쇼케이스 항상 표시 + 768px 타이틀 글리치 수정(2건)**: (1) 2단 브레이크포인트 1400→640px로 낮춰 쇼케이스를 항상 노출 — 콘텐츠가 넘치면 `safe center`가 시작점 정렬로 폴백해 폼은 좌측 온전, 쇼케이스만 우측으로 잘림(`overflow:hidden`이라 좌우 스크롤 없음). 폼 폭 고정 후 `safe center`가 정상 동작 확인. (2) 760~770px에서 "Login"이 좌측으로 밀리던 글리치 — keycloak.v2 styles.css가 `@media(min-width:768px)`에서 `__main-header`를 `grid-template-columns:70% 30%`로 바꿔 타이틀이 70% 트랙 안에서 중앙정렬되던 것 → `grid-template-columns:1fr !important`로 단일 열 고정. 768/900/1900px 검증. WHY: 쇼케이스 상시 노출 + 헤더 정렬 글리치 제거.
- **쇼케이스 이미지 등록 절차 문서**: `docs/showcase-images.md` 추가 — PNG 추가/삭제(파일명→타이틀, ~1280×800), `gen-services.sh`로 manifest 재생성, 커밋·서버 반영 절차. WHY: 연결 서비스 이미지를 직접 등록할 수 있게 절차 명문화.
- **로그인 박스 폭 고정·중앙정렬·패딩(3건)**: (1) 입력박스 위 패딩 — 라벨 하단 간격 12px. (2) login__container 내용 중앙정렬 — 타이틀·라벨 center(지난 좌측정렬·들여쓰기 되돌림). (3) 폼이 뷰포트 넓어질수록 커지던 문제 수정 — 원인은 PatternFly `.pf-v5-c-login__main`의 반응형 `max-inline-size`(1200px 브레이크포인트). `__container` 384px·`__main` 336px를 `width/max-width/max-inline-size !important`로 고정. 추가로 2단(폼+쇼케이스) 브레이크포인트를 901→1400px로 올려 그 미만에선 폼 단독 중앙(쇼케이스가 폼 좌측을 자르던 현상 제거). 1200/1320/1900px 스크린샷 검증. WHY: 폼을 항상 동일한 작은 크기로 고정하고 중앙 정렬.
- **로그인 박스 정리·에러 표시 보정(6건)**: (1) 로그인 박스 반응형 제거(640px 미디어쿼리 삭제, 항상 고정 소형). (2) 스크롤바 항상 숨김 — `html.login-pf, body.apple-login { overflow: hidden }`(쇼케이스 이미지 렌더 시 스크롤바 깜빡이며 레이아웃 흔들리던 문제). (3) 타이틀 좌측 기준을 입력칸 outer edge에 맞추고(들여쓰기 0), 라벨(`.pf-v5-c-form__label-text`)은 pill 곡선 끝(22px)에서 시작. (4) username 라벨 → `AD account`(중복 방지로 동일 placeholder 제거). (5) 인증 실패 메시지 `invalidUserMessage` → `Invalid id or password`(messages_en). (6) 에러 느낌표 아이콘 세로 중앙정렬 — PatternFly가 `.pf-v5-c-form-control__utilities`를 absolute로 둬 어긋난 것을 static+flex로 교정, 입력칸도 `width:100%`→`flex:1`로 아이콘 자리 확보. 로컬 일반/에러(잘못된 로그인 POST) 화면으로 검증. WHY: 로그인 박스 안정화 및 에러 UI 정리.
- **쇼케이스 부채꼴 방향/닷/좌측 잘림 보정(3건)**: (1) 부채꼴을 우상향으로(명시적 translate 오프셋 +X 우/-Y 상 + 시계방향 rotate, transform-origin 중심으로 환원), 낙하는 우하향으로(`DROP_X` 추가, 시계방향 tumble). (2) 창 프레임 신호등 닷을 타이틀바 오른쪽으로 이동(DOM에서 title 뒤로) + 색 순서 반대(green-yellow-red)로 — macOS 연상 제거. (3) `.pf-v5-c-login` `justify-content: center`→`safe center` + 좌우 48px 패딩 — 콘텐츠가 뷰포트보다 넓을 때 중앙 오버플로가 시작점(왼쪽=폼)을 잘라먹던 현상 해결(넘치면 start 정렬로 폴백, 폼 항상 노출). 폼 컬럼은 `flex:0 0 384px` 고정. 1050/1500px 스크린샷 검증. WHY: 부채꼴 방향·프레임 톤·좁은 창 폼 잘림 수정.
- **쇼케이스 부채꼴 + 드롭아웃 회전**: 우측 서비스 스택을 키우고(카드 620px, 패널 660px) 부채꼴로 펼침(`transform-origin: 50% 165%` 아래 pivot + 슬롯당 7° rotate). 회전 교체를 "맨 위 카드를 아래로 잡아내리며 회전→완전 페이드아웃→뒤로 재투입"으로 변경(`showcase.js`). 드롭은 `cubic-bezier(0.7,0,0.3,1)`(slow-fast-slow) 950ms, opacity 0까지 떨어진 뒤(setTimeout)에만 뒤 슬롯으로 스냅·재페이드인 → 기존 잔상(뒤로 넘어가며 남던 ghost) 제거. 로컬 스크린샷으로 부채꼴·교체·잔상 제거 검증. WHY: 더 역동적인 쇼케이스 + 잔상 버그 수정.
- **로그인 디자인 2차 조정(7건)**: (1) 비번 토글을 press-and-hold로 변경 — `password-hold.js` 신설(누르는 동안만 평문), `field.ftl` 버튼을 `data-password-hold`로. (2) 눈 버튼 잔여 보더/파란줄 제거 — PatternFly v5 버튼은 테두리를 `::after`로 그리므로 `.pf-m-control::after` kill(이전 `box-shadow` 가설 정정). (3) 라벨 `form__label-text` 18px/600. (4) 제목 좌측 정렬 + pill 곡선 끝(반경 22px)에 맞춘 들여쓰기. (5) username placeholder `AD account`. (6) password placeholder 채워진 동그라미 `●●●●●●●●`. (7) form-group 간격 16→8px·Sign In 버튼 margin-top 8→4px, rememberMe 비활성 시 빈 group 조건부 렌더로 잔여 간격 제거. 로컬 스크린샷 + HTML 배선 검증. WHY: 사용자 요청 디자인 미세 조정.
- **쇼케이스 덱 로드 무작위화 + URL 핀**: `showcase.js`에 Fisher-Yates `shuffle` 추가 — 로드마다 카드 덱을 무작위 순서로(기존 manifest 파일명 알파벳순 고정 → 랜덤). 더해 `?showcase=<title|filename>` 쿼리 파라미터로 특정 카드를 front에 **고정**(`pinnedFront`, 대소문자 무시·`.png` 제거 매칭, 못 맞추면 0=무작위 폴백). Keycloak 영향 검증: 모르는 auth-endpoint 파라미터는 무시돼 로그인 폼 정상 렌더(HTTP 200), 유효 PKCE 요청이면 `showcase=`가 로그인 페이지 URL에 잔존해 JS가 읽음 → 핀 동작. headless 캡처로 셔플(Mail/Calendar/AI Portal 등 front 변동)·핀(`showcase=Dashboard`→Dashboard front) 확인. 연동 사이트 담당자용 안내문 `docs/showcase-pin-guide.md` 신설(인증 요청 URL에 `&showcase=X` 추가법·스택별 예시·동작 규칙·PKCE FAQ·로컬 테스트 링크). WHY: 로그인마다 다른 서비스가 앞에 보이게 하고, 필요 시 특정 서비스를 URL로 고정할 수 있게.
- **쇼케이스 일시정지 컨트롤 + 2바퀴 자동정지**: `showcase.js`에 pause/play 토글 버튼 추가(`.sw-pause`, 인라인 SVG 두 아이콘). 쇼케이스 영역에 마우스 진입 후 **0.5초**면 버튼 fade-in(`syncVisible`은 `paused || hoverShown` 기준), 클릭 시 회전 토글(`setInterval` 콜백 선두에서 `if(paused)return`). **일시정지 상태면 호버 없이도 ▶ 버튼 상시 표시**. 더해 드롭 카운터로 **매 2바퀴(2×N 드롭)마다 자동정지**(`drops>=AUTO_PAUSE_AFTER → drops=0; setPaused(true)`, 반복) — 재생하면 또 2바퀴 후 정지. CSS는 `custom.css` 쇼케이스 섹션에 `.sw-pause`(덱 하단중앙·반투명+blur pill·hover scale 1.08·아이콘 토글) 추가, reduced-motion 시 transform 트랜지션 제거. headless로 DOM 배선(카드5+버튼/아이콘 생성, JS 무에러)·버튼 두 상태 시각·라이브 JS/CSS 서빙 확인. 호버/타이머/자동정지 같은 시간·상호작용 동작은 headless 한계로 코드 검증(브라우저 수동 확인 권고). WHY: 사용자가 카드를 멈춰 보거나 자동 회전을 일정 시점에 멈추게 하기 위함.
- **username 입력 placeholder 추가**: `login.ftl`의 username `<@field.input>` 호출에 `placeholder="Enter your AD account"` 추가(라벨은 그대로 'AD account'). `field.ftl` input 매크로는 이미 placeholder 지원 — password처럼 호출 시 넘기기만 하면 됨. 렌더 HTML에서 username input에 placeholder 출력 확인. WHY: 빈 입력칸에 입력 안내가 없던 것 보완.

## 2026-06-08

- **쇼케이스 이미지 v1 빈 하단 밴드 결함 수정**: 기존 4종(Analytics·Calendar·Dashboard·Mail)이 콘텐츠가 상단 ~73%(17~626px)에만 있고 하단 ~173px가 빈 배경 밴드였던 결함 수정. 원인: 목업 HTML이 800px 높이를 채우지 않고 상단 고정. 동일 스켈레톤(헤더+타이틀+3 stat 카드+차트 패널)·brand색(v1 PNG에서 픽셀 추출: 보라/코랄/블루/틸)을 유지하되, html/body를 정확히 1280×800·`overflow:hidden`으로 두고 `.app`을 `height:100%` flex 컬럼, 차트 패널을 `flex:1`로 늘려 하단 ~34px까지 채움(상·하 여백 균등). Chrome headless(puppeteer로 설치)로 재캡처 후 **하단 120px 밴드 평균색 vs 코너 배경색 비교 + 상/하 빈 여백 ≤80px 검증**(스크립트 자동) → bottomGap 173px→34px·colordiff 93~124 통과, 4장 시각 확인. `gen-services.sh`로 manifest 재생성(파일명 동일·변화 없음). `docs/showcase-image-prompt.md`에 "캔버스 전체 높이 채움(html/body 1280×800·root 100%·flex 컬럼·상하 여백 ≤~40px)" 요구사항·"하단 밴드 평균색 검증" 단계·체크리스트 항목 추가해 재발 방지. WHY: 로그인 카드 슬롯(aspect 1280/800 고정)에서 하단 빈 흰 밴드가 어색하게 보이던 문제 제거.
- **프롬프트 — 가운데 모달형 레이아웃 배경 여백 가드 추가**: `docs/showcase-image-prompt.md`에 "콘텐츠가 가운데 모달/다이얼로그/카드형이면 모달이 캔버스 대부분(각 변 ≥ ~70%)을 차지하고 바깥 배경 프레임은 각 변 ≤ ~15%로 유지(작은 모달을 큰 빈 배경에 띄우지 말 것)" 요구사항을 #1에 추가. 검증 섹션엔 모달형 예외(하단 밴드 검증 건너뛰고 모달 바운딩박스가 폭·높이 ≥ ~70%인지 확인)와 체크리스트 항목 추가. WHY: 세로 적층형만 가정하던 "전체 높이 채움" 규칙이 가운데 모달 UI에선 부적합 — 모달 주변 배경이 과도하게 커지는 것을 막고, 모달형 배경 여백이 하단 밴드 검증에 빈 밴드로 오탐되던 모순도 해소.
- **프롬프트 — 검증 corner 가정 결함 수정 + 사이드바/그리드 채움 지침**: AI-portal 쇼케이스가 새 프롬프트로도 하단 ~156px 흰 밴드(잘린 느낌)가 났음. 실패 이미지 분석으로 원인 2개 확인: (1) 검증이 `corner(4,4)`를 배경색으로 가정했는데, 풀폭 검은 헤더가 있어 corner=검정 → 흰 하단 밴드와 색차가 커 "빈 밴드 아님"으로 **오탐 통과**. (2) 사이드바+카드 그리드(6개·3행)가 캔버스보다 짧은데 패널이 안 늘어나 바닥에 흰 띠. 수정: 검증을 **행/열 variance 기반 콘텐츠 바운딩박스**로 교체(고정 corner 샘플 폐기 — 풀폭 헤더가 있어도 견고), 빈 여백 임계값 80→40px. 요구사항 #1에 "사이드바·콘텐츠 둘 다 height:100%, 카드 그리드는 `grid-auto-rows:1fr`로 행 stretch 또는 행 추가, body 배경은 1280×800 전체 페인트(하단 순백 띠도 동일 결함)" 추가. 실제 실패 이미지로 새 검증기가 FAIL(bottomGap 156·높이 77%)로 잡는 것 확인 → 동일 레이아웃을 채움 기법으로 재구성해 bottomGap 34·높이 92%·OK 재캡처로 기법 검증. WHY: 풀폭 헤더 레이아웃에서 검증이 무력화돼 잘린 이미지가 계속 통과하던 근본 원인 제거.
