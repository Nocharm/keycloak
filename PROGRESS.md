# Progress

프로젝트 진행 현황 로그. 커밋 직전 갱신한다 (`rules/common/git.md` 규칙).

## 2026-06-03

- **템플릿 셋업** (`/setup-from-template`): 미사용 언어 룰(python/typescript) import·섹션 제거, 상단 seed 주석 제거, `docs/template/`·`rules/languages/` 삭제. @import 12개 검증 OK.
- **Keycloak 로컬 개발 배포 설계 확정**: Docker Compose 2-서비스(keycloak `start-dev` + postgres, named volume 영속), realm 빈 상태 시작·수동 구성, secret은 `.env` 주입. spec: `docs/superpowers/specs/2026-06-03-keycloak-docker-local-dev-design.md`. WHY: 로컬에서 앱 OIDC 연동을 운영과 동일 DB로 재현 가능하게 하기 위함.
- **구현 플랜 작성**: 6-task 플랜(env.example → .env → compose → 기동·로그인 검증 → 영속 검증 → 기록). plan: `docs/superpowers/plans/2026-06-03-keycloak-docker-local-dev.md`.
- **구현 시작**: `.env.example` 추가 (이미지 핀·관리자·DB 자격 키 정의). secret 실값은 `.env`(미커밋)로 주입.
- **compose 추가**: `docker-compose.yml` — postgres(healthcheck·volume) + keycloak(start-dev, depends_on healthy, 8080/9000). `docker compose config` 통과.
- **런타임 검증 완료**: `up -d` → postgres healthy / keycloak running, `/health/ready` UP, master realm 토큰 발급(관리자 로그인) OK, realm 생성→`restart keycloak`→잔존(200) 으로 영속 검증. 테스트 realm 정리(204).
- **README 작성**: 플레이스홀더 1줄 → 전체 문서(구성표·빠른 시작·환경 변수표·명령어·헬스 체크) 로 교체.
- **SSO 동작 GUI 검증**: `demo` realm + 테스트 사용자 생성, OIDC client 2개(`site1` localhost:3000 / `site2` localhost:3001, public·standard flow) 등록. site1 로그인 → site2 자동 로그인(SSO) 및 site1 로그아웃 → site2 세션 종료(Single Logout) 확인. WHY: 단일 realm으로 다중 앱 세션 공유가 실제로 동작하는지 로컬에서 확인하기 위함.
- **커스텀 로그인 테마 추가**: compose `keycloak` 서비스에 `./themes:/opt/keycloak/themes` 볼륨 마운트(다른 서비스/설정 불변). `themes/custom/login` 테마 생성 — `parent=keycloak.v2`, `styles=css/styles.css css/custom.css`(v2 베이스 스타일 유지 위에 Apple 스타일 오버라이드), v2 `login.ftl`/`template.ftl` 사본(검증 마커 + `apple-login` body 클래스), `custom.css`는 `docs/superpowers/apple.md` 토큰(Action Blue·SF Pro·pill CTA) 구현. `demo` realm `loginTheme=custom` 설정(kcadm). 검증: site1 로그인 폼 HTML에 `apple-login` body·마커·`styles.css`→`custom.css` 순서 확인, 테마 CSS 200 서빙. WHY: 내장 jar 테마 불변 유지하며 호스트 오버라이드로 로그인 화면 브랜딩.
- **로그인 테마 세부 조정**: `AI-portal/src/app/admin/login/page.tsx`(+ tailwind.config·apple.css) 기준으로 `custom.css` 재작성 — 카드 제거(맨 surface-alt 배경·max-w 384px), 입력 pill·h44·focus 테두리만 accent, 중앙 display-md(34px) 타이틀, fade-up 스태거 진입(ease-spring), eye 토글 ghost 버튼. Chrome headless 스크린샷으로 시각 검증.
- **연결 서비스 쇼케이스 스택 추가**: 로그인 화면을 2-컬럼으로(좌 폼 불변 + 우 쇼케이스). 우측에 브라우저 창 모양 카드를 겹쳐 쌓고 `showcase.js`가 `manifest.json`을 fetch해 렌더 후 3.5s 간격 맨 위 카드를 뒤로 보내는 오토 로테이션. `gen-services.sh`가 `resources/img/services/*.png`를 스캔해 파일명→창 타이틀 매핑으로 `manifest.json` 자동 생성(PNG 추가/삭제 후 재실행만 하면 카드 증감). 목업 PNG 4종은 headless Chrome 렌더로 생성(Python 의존성 0). 검증: 리소스 200 서빙, 좌 폼+우 스택 스크린샷, 회전 동작, 4↔5 증감, ≤900px 우측 패널 숨김. WHY: 어떤 서비스들이 이 SSO로 연결되는지 로그인 시점에 시각적으로 보여주기 위함.
- **서버 배포 플로우 문서화 + CRLF 고정 + 포트 파라미터화**: `.gitattributes` 추가(`* eol=lf`, `*.sh eol=lf`, `*.png binary`) — Mac→Windows→Linux 서버 경로에서 Windows 체크아웃(autocrlf)이 셸 스크립트 셰뱅을 CRLF로 깨뜨리는 것을 working tree LF 강제로 차단. `KEYCLOAK_HTTP_PORT`(.env, 기본 8080) 도입해 compose 포트를 `${KEYCLOAK_HTTP_PORT:-8080}:8080`로 파라미터화(서버 6502). README에 배포 경로(git push→pull→`scp -r`→서버 `.env.example`로 작성→`up -d`) 추가 — `.env`는 gitignore라 scp에 자연 제외·`.git` 포함. WHY: 로컬과 동일 리포를 서버에 안전히 옮겨 포트만 바꿔 기동하기 위함.
- **README 배포 절차 보강(권한·용량 우회 + 반복 작업 명령)**: scp를 폴더 이동 후 상대경로(`scp -r .`)로 변경. SSH 서버 권한 문제로 홈(`~/keycloak_dev`) 업로드 → `sudo mv`(최초)/`sudo rsync -a --delete --exclude='.env'`(이후)로 `/data/keycloak_dev`(용량) 동기화하는 2-경로 명령 추가. 반복 업데이트 시 `.env`·DB 볼륨 보존. WHY: 서버의 쓰기 권한/디스크 제약과 반복 배포를 실제 운영 절차에 맞춰 문서화.
- **서버 운영 명령어 문서 신설**: `docs/server-commands.md` 추가 — 서버에서 복붙용 명령 모음(`"HTTPS required"` 해제 `sslRequired=NONE` kcadm, 테마 적용 `loginTheme=custom`, 상태/로그/헬스, 재기동). 코드블록별 복사 버튼 활용. 앞으로 운영 명령은 이 문서에 누적. WHY: 서버에서 kcadm/compose 명령 복사가 번거로워 단일 복붙 시트 필요.
- **로그인 디자인 세부 조정 + 디자인 규칙 문서화**: (1) 제목 `Sign in to your account`→`Login` (`messages/messages_en.properties`로 `loginAccountTitle` 오버라이드). (2) 입력칸 placeholder 추가 — `field.ftl`을 테마로 복사해 `input`/`password` 매크로에 `placeholder` 파라미터 신설. (3) 비번 보기 버튼 24px로 축소 + 보더 제거. (4) `docs/design-rules.md` 추가 — claude_design_template `.claude/design/styling-rules.md`·apple.md 기준 핵심 규칙 요약. 로컬 8080 데모 realm 스크린샷으로 검증. WHY: 로그인 화면을 참고 디자인에 맞춰 단순화하고 규칙을 명문화.
- **로그인 박스 폭 고정·중앙정렬·패딩(3건)**: (1) 입력박스 위 패딩 — 라벨 하단 간격 12px. (2) login__container 내용 중앙정렬 — 타이틀·라벨 center(지난 좌측정렬·들여쓰기 되돌림). (3) 폼이 뷰포트 넓어질수록 커지던 문제 수정 — 원인은 PatternFly `.pf-v5-c-login__main`의 반응형 `max-inline-size`(1200px 브레이크포인트). `__container` 384px·`__main` 336px를 `width/max-width/max-inline-size !important`로 고정. 추가로 2단(폼+쇼케이스) 브레이크포인트를 901→1400px로 올려 그 미만에선 폼 단독 중앙(쇼케이스가 폼 좌측을 자르던 현상 제거). 1200/1320/1900px 스크린샷 검증. WHY: 폼을 항상 동일한 작은 크기로 고정하고 중앙 정렬.
- **로그인 박스 정리·에러 표시 보정(6건)**: (1) 로그인 박스 반응형 제거(640px 미디어쿼리 삭제, 항상 고정 소형). (2) 스크롤바 항상 숨김 — `html.login-pf, body.apple-login { overflow: hidden }`(쇼케이스 이미지 렌더 시 스크롤바 깜빡이며 레이아웃 흔들리던 문제). (3) 타이틀 좌측 기준을 입력칸 outer edge에 맞추고(들여쓰기 0), 라벨(`.pf-v5-c-form__label-text`)은 pill 곡선 끝(22px)에서 시작. (4) username 라벨 → `AD account`(중복 방지로 동일 placeholder 제거). (5) 인증 실패 메시지 `invalidUserMessage` → `Invalid id or password`(messages_en). (6) 에러 느낌표 아이콘 세로 중앙정렬 — PatternFly가 `.pf-v5-c-form-control__utilities`를 absolute로 둬 어긋난 것을 static+flex로 교정, 입력칸도 `width:100%`→`flex:1`로 아이콘 자리 확보. 로컬 일반/에러(잘못된 로그인 POST) 화면으로 검증. WHY: 로그인 박스 안정화 및 에러 UI 정리.
- **쇼케이스 부채꼴 방향/닷/좌측 잘림 보정(3건)**: (1) 부채꼴을 우상향으로(명시적 translate 오프셋 +X 우/-Y 상 + 시계방향 rotate, transform-origin 중심으로 환원), 낙하는 우하향으로(`DROP_X` 추가, 시계방향 tumble). (2) 창 프레임 신호등 닷을 타이틀바 오른쪽으로 이동(DOM에서 title 뒤로) + 색 순서 반대(green-yellow-red)로 — macOS 연상 제거. (3) `.pf-v5-c-login` `justify-content: center`→`safe center` + 좌우 48px 패딩 — 콘텐츠가 뷰포트보다 넓을 때 중앙 오버플로가 시작점(왼쪽=폼)을 잘라먹던 현상 해결(넘치면 start 정렬로 폴백, 폼 항상 노출). 폼 컬럼은 `flex:0 0 384px` 고정. 1050/1500px 스크린샷 검증. WHY: 부채꼴 방향·프레임 톤·좁은 창 폼 잘림 수정.
- **쇼케이스 부채꼴 + 드롭아웃 회전**: 우측 서비스 스택을 키우고(카드 620px, 패널 660px) 부채꼴로 펼침(`transform-origin: 50% 165%` 아래 pivot + 슬롯당 7° rotate). 회전 교체를 "맨 위 카드를 아래로 잡아내리며 회전→완전 페이드아웃→뒤로 재투입"으로 변경(`showcase.js`). 드롭은 `cubic-bezier(0.7,0,0.3,1)`(slow-fast-slow) 950ms, opacity 0까지 떨어진 뒤(setTimeout)에만 뒤 슬롯으로 스냅·재페이드인 → 기존 잔상(뒤로 넘어가며 남던 ghost) 제거. 로컬 스크린샷으로 부채꼴·교체·잔상 제거 검증. WHY: 더 역동적인 쇼케이스 + 잔상 버그 수정.
- **로그인 디자인 2차 조정(7건)**: (1) 비번 토글을 press-and-hold로 변경 — `password-hold.js` 신설(누르는 동안만 평문), `field.ftl` 버튼을 `data-password-hold`로. (2) 눈 버튼 잔여 보더/파란줄 제거 — PatternFly v5 버튼은 테두리를 `::after`로 그리므로 `.pf-m-control::after` kill(이전 `box-shadow` 가설 정정). (3) 라벨 `form__label-text` 18px/600. (4) 제목 좌측 정렬 + pill 곡선 끝(반경 22px)에 맞춘 들여쓰기. (5) username placeholder `AD account`. (6) password placeholder 채워진 동그라미 `●●●●●●●●`. (7) form-group 간격 16→8px·Sign In 버튼 margin-top 8→4px, rememberMe 비활성 시 빈 group 조건부 렌더로 잔여 간격 제거. 로컬 스크린샷 + HTML 배선 검증. WHY: 사용자 요청 디자인 미세 조정.
